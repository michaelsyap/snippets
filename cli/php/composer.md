# Composer CLI Snippets

####  To regenerate the list of all classes that need to be included in the project
```bash
composer dump-autoload --optimize
```



####  To install composer packages
```bash
composer install
```